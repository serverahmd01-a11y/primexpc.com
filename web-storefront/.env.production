# Production build config (used automatically by `npm run build`).
# The storefront is served by the backend on the SAME origin,
# so a relative /api path is used (works on any domain).
VITE_API_URL=/api

VITE_GA_MEASUREMENT_ID=
